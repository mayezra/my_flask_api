apiVersion: v1
kind: Service
metadata:
  name: locust
spec:
  type: NodePort   # use LoadBalancer if on cloud
  ports:
  - port: 8089
    targetPort: 8089
    nodePort: 30089   # optional, pick a free port
  selector:
    app: locust


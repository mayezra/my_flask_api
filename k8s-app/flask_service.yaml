apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  ports:
    - name: http
      port: 5000
      targetPort: 5000
    - name: https
      port: 443
      targetPort: 443
  selector:
    app: flask-api
  type: ClusterIP


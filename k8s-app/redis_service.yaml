apiVersion: v1
kind: Service
metadata:
  name: redis-db
spec:
  ports:
    - protocol: TCP
      port: 6379  # Exposes Redis port
      targetPort: 6379
  clusterIP: None  # Headless service for direct connections
  selector:
    app: redis  # Matches the label in our Deployment
